grammarId: default
version: "1.1"
rules:
  - ruleId: R-12
    priority: 1
    specificity: 12
    pattern: 'publish policy <policy_id> allow ledger commit'
    template:
      action: publish_policy
      params:
        policy_id: $policy_id
        effect: allow
        capability: ledger.commit
